{{- if .Values.ayon.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "ayon.fullname" . }}
  labels:
    {{- include "ayon.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "ayon.fullname" . }}
  minReplicas: {{ .Values.ayon.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.ayon.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.ayon.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.ayon.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.ayon.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.ayon.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
